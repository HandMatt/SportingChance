# Policy PDFs (`static/pdfs/`)

Drop the four policy PDFs here using these **exact** filenames (paths are hard-coded in `content/policies/*.md`):

| File | Policy page |
|------|-------------|
| `safeguarding.pdf` | `/policies/safeguarding-policy/` |
| `health-and-safety.pdf` | `/policies/health-and-safety-policy/` |
| `anti-bullying.pdf` | `/policies/anti-bullying-policy/` |
| `complaints.pdf` | `/policies/complaints-procedure/` |

Temporary: tiny placeholder PDFs (~600 bytes) so the viewer can be smoke-tested
without bloating git/Docker sync. Replace each with the real policy document when ready.

Do not rename without also updating front matter `pdf_src` on the matching policy page.
