# Restore accessible policy documents (OneDrive / SharePoint)

**Status:** Superseded — public SharePoint/OneDrive iframes cannot work for anonymous visitors (Microsoft login / portal redirects).

**Replacement:** Local PDFs + PDF.js — see root [`POLICY_PDFS.md`](../../POLICY_PDFS.md) (`feature/policy-pdfjs-viewer`).

SharePoint remains the staff CMS; site copies live in `static/pdfs/`. Task B (Power Automate → GitHub) is planned in `POLICY_PDFS.md` for when IT is ready.
