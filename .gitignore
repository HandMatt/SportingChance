/node_modules
/public
.hugo_build.lock
.cache
dist/
# Root-only: an unanchored `build/` would also match static/pdfjs/build/.
# Negations (if needed) must come *after* the ignore pattern — last match wins.
/build/
!static/pdfjs/build/
!static/pdfjs/build/**
.env
.env.local
.env.*.local
.yarn/install-state.gz
